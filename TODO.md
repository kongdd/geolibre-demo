# TODO

## GEE 代理安全

- [ ] 为 `/project-demo/api/ee/*` 增加真正的用户认证；`Origin` 校验不能替代认证。
- [ ] 限制可执行表达式与数据集，避免借用个人凭证访问私有资产或消耗配额。
- [ ] 改用最小权限服务账号，并设置独立项目与配额。
- [ ] 在 Caddy 按可信客户端 IP 限流；当前单进程限流仅能缓解滥用。

验收：未认证及伪造 `Origin` 请求均被拒绝；授权用户可加载 PML；响应、构建产物和日志不含 OAuth token。
